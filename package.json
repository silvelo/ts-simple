{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "tsc": "lerna run tsc",
    "test": "lerna run test",
    "coverage": "lerna run coverage",
    "lerna:version": "lerna version --exact",
    "prepublish": "npm run tsc",
    "lerna:publish": "lerna publish from-git --yes",
    "lerna:badges": "lerna exec -- cross-var node $LERNA_ROOT_PATH/scripts/generate.js"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/node": "^11.13.4",
    "chai": "^4.2.0",
    "coveralls": "^3.0.3",
    "cross-var": "^1.1.0",
    "generator-badge": "^1.2.0",
    "jest": "^24.7.1",
    "lerna": "^3.13.2",
    "ts-jest": "^24.0.2",
    "tslint": "^5.15.0",
    "typescript": "^3.4.3",
    "replace-in-file": "^3.4.4",
    "shields": "^2.1.0"
  }
}
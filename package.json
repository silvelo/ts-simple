{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "tsc": "lerna run tsc",
    "test": "lerna run test",
    "coverage": "lerna run coverage",
    "prepublish": "npm run tsc",
    "preversion": "npm run lern:badges",
    "postversion": "echo postverion",
    "lerna:version": "lerna version --exact --no-push",
    "lerna:publish": "lerna publish from-git --yes",
    "lerna:badges": "lerna exec -- cross-var node $LERNA_ROOT_PATH/scripts/generate-badges.js"
  },
  "devDependencies": {
    "@microsoft/tsdoc": "^0.12.9",
    "@types/jest": "^24.0.11",
    "@types/node": "^11.13.4",
    "coveralls": "^3.0.3",
    "cross-var": "^1.1.0",
    "jest": "^24.7.1",
    "lerna": "^3.13.2",
    "ts-jest": "^24.0.2",
    "tslint": "^5.15.0",
    "typescript": "^3.4.3",
    "update-section": "^0.3.3"
  }
}
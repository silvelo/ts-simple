{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "tsc": "lerna run tsc",
    "test": "lerna run test",
    "coverage": "lerna run coverage",
    "lerna:version": "lerna version --exact",
    "prepublish": "npm run tsc",
    "travis:login": "npm run npm-login-noninteractive",
    "publish": "lerna publish from-git --yes"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/node": "^11.13.4",
    "chai": "^4.2.0",
    "coveralls": "^3.0.3",
    "jest": "^24.7.1",
    "lerna": "^3.13.2",
    "ts-jest": "^24.0.2",
    "tslint": "^5.15.0",
    "typescript": "^3.4.3"
  }
}